
import React, { memo } from 'react';  // Correct import

export default memo(function ProductList({ product }) {
  return (
    <div>
      {/* Render the product details here */}
    </div>
  );
});
